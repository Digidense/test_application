apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: http-app-ingress
  namespace: test-app
  annotations:
spec:
  rules:
    - host: digidense.xyz
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: nginx-service
                port:
                  number: 80
